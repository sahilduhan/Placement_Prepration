#include<bits/stdc++.h>
using namespace std;
vector <int> dijkstra(int n, vector<vector<int>> adj [], int S){
    set<pair<int, int>>st;
    vector<int>dist(n, 1e9);
    st.insert({0, S});
    dist[S] = 0;

    while (!st.empty()){
        auto it = *(st.begin());

    }

}
int main(){



    return 0;
}